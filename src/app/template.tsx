'use client';

// All scroll and page transition logic has been moved to the main page
// to create a unified, scroll-driven animation experience.

export default function Template({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
